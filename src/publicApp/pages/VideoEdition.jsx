import { CardsGrid } from "../components/card";

export const VideoEdition = () => (
  <CardsGrid
    group="video edition"
    sxContainer={{
      minHeight: "100vh",
      backgroundColor: "primary.main",
      padding: 4,
    }}
  />
);
